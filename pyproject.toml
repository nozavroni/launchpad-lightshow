[project]
name = "launchpad-lightshow"
version = "0.1.0"
description = "Quick dashboard for Novation Launchpad X light scenes"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "launchpad-py>=0.8.0",
  "mido>=1.3.2",
  "python-rtmidi>=1.5.8",
]

[tool.ruff]
line-length = 100
